

I have two documents I need you to compare precisely.

DOCUMENT A — REFERENCE WORKFLOWS
This is the complete list of flows a top-tier tournament manager app should have.
It is organised into 12 sections with numbered flows (e.g. 3.4, 5.2).

[PASTE REFERENCE DOCUMENT HERE]

---

DOCUMENT B — MY APP'S CURRENT FLOWS
This is an audit of what my Django app currently implements, produced by reading
the actual codebase including models.py, views.py, urls.py, and templates.

[PASTE YOUR APP AUDIT HERE]

---

--------------------------------------------------------------------------------
YOUR TASK
--------------------------------------------------------------------------------

Compare Document A against Document B flow by flow and produce a single
structured comparison report in plain text using the format below.

Do not summarise. Do not skip any flow from Document A.
Work through every section and every numbered flow in order.

For each flow in Document A, check whether it exists in Document B and assign
one of these four statuses:

  COMPLETE   — fully implemented, permission checks present, no obvious gaps
  PARTIAL    — exists in code but missing something (e.g. no permission check,
                no error handling, no notification sent, redirect is wrong,
                model field not being set)
  STUB       — URL or template exists but view does nothing meaningful yet
  MISSING    — no trace of this flow anywhere in the codebase

--------------------------------------------------------------------------------
REPORT FORMAT — use this exactly for every flow
--------------------------------------------------------------------------------

[SECTION NUMBER] [SECTION NAME]
---------------------------------------------
Flow [ID]: [Flow name from Document A]
Status: COMPLETE | PARTIAL | STUB | MISSING
Actor: [who should trigger it]
Reference says:
  [1-2 line summary of what Document A requires]
App currently does:
  [1-2 line summary of what Document B shows, or "Nothing found" if missing]
Gaps:
  - [specific thing missing or wrong, one per line]
  - [if Status is COMPLETE, write: None]
Code location:
  [view function name and file, or "Not found"]

---------------------------------------------

Repeat the above block for every flow. Do not skip any.

--------------------------------------------------------------------------------
AFTER ALL FLOWS — APPEND THESE THREE SUMMARY SECTIONS
--------------------------------------------------------------------------------

================================================================================
SUMMARY — COUNTS
================================================================================
Total flows in reference:        [number]
Complete:                         [number]  ([percent]%)
Partial:                          [number]  ([percent]%)
Stub:                             [number]  ([percent]%)
Missing:                          [number]  ([percent]%)

================================================================================
SUMMARY — PRIORITY IMPLEMENTATION ORDER
================================================================================
List every MISSING and PARTIAL flow in the order they should be built,
following this priority logic:

  Priority 1 — MISSING flows that block other flows from working
               (e.g. if registration is missing, matches cannot exist)
  Priority 2 — PARTIAL flows with missing permission checks
               (security gaps — fix these before shipping)
  Priority 3 — PARTIAL flows with missing model writes or audit logging
  Priority 4 — PARTIAL flows with missing notifications or error handling
  Priority 5 — MISSING flows that are independent and can be added later

For each item in the list write:
  [Priority] Flow [ID]: [name] — [one sentence on what specifically to fix]

================================================================================
SUMMARY — PERMISSION GAPS ONLY
================================================================================
List every flow where the permission check is absent or wrong.
This is a separate focused list for security review.

Format:
  Flow [ID]: [name]
  Current check: [what the code does, e.g. @login_required only]
  Required check: [what Document A says it should be]
  Risk: [what a user could do if this is not fixed]

================================================================================
END OF PROMPT
================================================================================